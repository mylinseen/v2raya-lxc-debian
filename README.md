# v2raya-lxc-debian

该项目旨在通过在 Debian 12 上配置 **v2rayA** 和 **sing-box** 实现透明代理，作为 LXC 容器中的旁路由。适用于具有简单需求的翻墙环境配置。

## 🚀 一键安装

只需要执行以下命令，即可自动化完成所有安装和配置：

```bash
bash <(curl -Ls https://raw.githubusercontent.com/mylinseen/v2raya-lxc-debian/main/install.sh)
